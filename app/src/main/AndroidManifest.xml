<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="BK Turizm"
        android:roundIcon="@drawable/app"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".biletGoster"></activity>
        <activity android:name=".yerSec" />
        <activity android:name=".seferEkle" />
        <activity android:name=".seferler" />
        <activity android:name=".yeniUye" />
        <activity android:name=".ListDataActivity" />
        <activity android:name=".bilgi" />
        <activity android:name=".rezervasyonlar" />
        <activity android:name=".biletlerim" />
        <activity android:name=".biletAl" />
        <activity android:name=".user" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>